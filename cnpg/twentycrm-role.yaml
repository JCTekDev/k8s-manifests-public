apiVersion: postgresql.cnpg.io/v1
kind: Role
metadata:
  name: twenty
spec:
  cluster:
    name: postgres-cluster1
  login: true
  superuser: false
  createdb: false
  # Secret deployed to postgres-infra by the cnpg-secrets ArgoCD app (wave 0).
  # Must exist before this Role is reconciled.
  passwordSecret:
    name: twentycrm-db-credentials
