apiVersion: v1
kind: Service
metadata:
  name: browserusemcp
  labels:
    app: browserusemcp
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: sse
  selector:
    app: browserusemcp
