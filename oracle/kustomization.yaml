apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml
  - statefulset.yaml

labels:
  - pairs:
      app.kubernetes.io/name: oracle-db-free
      app.kubernetes.io/component: database
    includeSelectors: true
